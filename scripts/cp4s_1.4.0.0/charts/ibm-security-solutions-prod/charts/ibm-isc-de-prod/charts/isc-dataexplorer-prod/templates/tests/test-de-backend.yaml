{{- $ndx := $.Values.global.images.de.backend -}}
{{- $image := printf "%s/%s:%s" (include "platform.repository" .) $ndx.image $ndx.tag -}}
apiVersion: v1
kind: Pod
metadata:
  name: debackend-test-ready
  labels:
    app: debackend-test
    app.kubernetes.io/name: debackend-test
    helm.sh/chart: debackend
    release: {{ $.Release.Name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  serviceAccountName: ibm-isc-application
  imagePullSecrets:
    - name: ibm-isc-pull-secret
  containers:
    - name: debackend-test
      image: {{ $image }}
      command: ["sh", "-c", "curl --cacert /etc/config/ca.crt https://debackend:3000/liveness"]
      resources:
        limits:
          memory: 100Mi
          cpu:  200m
        requests:
          memory: 100Mi
          cpu: 200m
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
          - ALL
        privileged: false
        readOnlyRootFilesystem: false
        runAsNonRoot: true
      volumeMounts:
        - mountPath: /etc/config
          name: secrets
          readOnly: true
  volumes:
    - name: secrets
      secret:
        defaultMode: 420
        secretName: debackend-tls
  hostIPC: false
  hostNetwork: false
  hostPID: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 1001
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                  - amd64
  restartPolicy: Never
