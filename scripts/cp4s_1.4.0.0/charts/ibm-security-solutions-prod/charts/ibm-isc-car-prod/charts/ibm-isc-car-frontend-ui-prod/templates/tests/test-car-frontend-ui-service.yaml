{{- $image := printf "%s/%s:%s" (include "platform.repository" .) $.Values.global.images.car.carFrontendUi.image $.Values.global.images.car.carFrontendUi.tag -}}
apiVersion: v1
kind: Pod
metadata:
  name: car-frontend-ui-test-ready
  labels:
    app.kubernetes.io/name: car-frontend-ui-test
    helm.sh/chart: {{ $.Chart.Name }}
    release: {{ $.Release.Name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  serviceAccountName: ibm-isc-application
  imagePullSecrets:
    - name: ibm-isc-pull-secret
  containers:
    - name: car-frontend-ui-test
      image: {{ $image }}
      command: ["sh", "-c", "curl -i --cacert /etc/config/ca.crt https://car-frontend-ui:3000/liveness"]
      resources:
        limits:
          memory: 100Mi
          cpu:  200m
        requests:
          memory: 100Mi
          cpu: 200m
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
          - ALL
        privileged: false
        readOnlyRootFilesystem: false
        runAsNonRoot: true
        runAsUser: 1001
      volumeMounts:
        - mountPath: /etc/config
          name: secrets
          readOnly: true
  volumes:
    - name: secrets
      secret:
        defaultMode: 420
        secretName: car-frontend-ui-tls
  hostIPC: false
  hostNetwork: false
  hostPID: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 1001
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                  - amd64
  restartPolicy: Never
