apiVersion: isc.ibm.com/v1
kind: ISCInventory
metadata:
  labels:
    instance: cases-dashboard-1
    sort: pulsetemplate
    app.kubernetes.io/name: {{.Chart.Name}}-{{.Chart.Version}}
    helm.sh/chart: {{ $.Chart.Name }}
    release: {{ $.Release.Name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
  name: cases-dashboard-1
spec:
  definitions:
    templateId: cases_dashboard_1
    templateJson: {{ .Files.Get "json_files/cases_dashboard_1.json" | toJson }}
labels:
  instance: cases-dashboard-1
  sort: pulsetemplate