# operator-sdk new isc-crunchy-postgresql-operator --api-version=isc.ibm.com/v1 --kind=CrunchyPostgresqlOperator --type=ansible --generate-playbook
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "ibm-cp4s-postgres-prod.name" . }}
  labels:
    name: {{ template "ibm-cp4s-postgres-prod.name" . }}
    release: {{ .Release.Name }}
    app.kubernetes.io/name: {{.Chart.Name}}-{{.Chart.Version}}
    helm.sh/chart: {{ $.Chart.Name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
{{- if .Values.postgresOperator.annotations }}
  annotations:
{{ toYaml .Values.postgresOperator.annotations | indent 4 }}
{{- end }}
spec:
  replicas: {{ .Values.postgresOperator.operator.replicas }}
  selector:
    matchLabels:
      name: {{ template "ibm-cp4s-postgres-prod.name" . }}
  template:
    metadata:
      labels:
        name: {{ template "ibm-cp4s-postgres-prod.name" . }}
        release: {{ .Release.Name }}
        chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/name: {{.Chart.Name}}-{{.Chart.Version}}
        helm.sh/chart: {{ $.Chart.Name }}
        app.kubernetes.io/instance: {{ $.Release.Name }}
        app.kubernetes.io/managed-by: {{ $.Release.Service }}
      annotations:
{{ toYaml .Values.postgresOperator.annotations | indent 8 }}
    spec:
      serviceAccountName: {{ template "ibm-cp4s-postgres-prod.name" . }}
      hostPID: false
      hostIPC: false
      hostNetwork: false
      dnsConfig:
        options:
          - name: single-request-reopen
      securityContext:
{{- if not (.Capabilities.APIVersions.Has "security.openshift.io/v1") }}
        runAsUser: {{ .Values.runAsUser }}
{{- end }}
        runAsNonRoot: true
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: beta.kubernetes.io/arch
                    operator: In
                    values:
                      - {{ $.Values.global.arch | default "amd64" }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - {{ template "ibm-cp4s-postgres-prod.name" . }}
              topologyKey: kubernetes.io/hostname
      containers:
        - name: operator
          {{- include "ibm-isc-cases.podcfg" (dict "val" .Values "app" "postgresOperator") | indent 6 }}
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: {{ template "ibm-cp4s-postgres-prod.name" . }}
            - name: ANSIBLE_GATHERING
              value: explicit
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          livenessProbe:
            exec:
              command:
                - /scripts/liveness-script.sh
          readinessProbe:
            exec:
              command:
              - /usr/bin/true
{{- if .Values.postgresOperator.operator.resources }}
          resources:
{{ toYaml .Values.postgresOperator.operator.resources | indent 12 }}
{{- end }}
      volumes:
        - name: runner
          emptyDir: {}
