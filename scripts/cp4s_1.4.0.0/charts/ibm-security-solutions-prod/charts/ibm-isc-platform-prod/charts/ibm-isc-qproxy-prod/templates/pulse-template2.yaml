apiVersion: isc.ibm.com/v1
kind: ISCInventory
metadata:
  labels:
    instance: qproxydashboard-2
    sort: pulsetemplate
    app.kubernetes.io/name: {{ .Chart.Name }}
    helm.sh/chart: {{ .Chart.Name }}
    release: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  name: qproxydashboard-2
spec:
  definitions:
    templateId: qproxy_pulse_dashboard_monitoring
    templateJson: {{ .Files.Get "json_files/siem_monitoring_dashboard.json" | toJson }}
labels:
  instance: qproxydashboard-2
  sort: pulsetemplate